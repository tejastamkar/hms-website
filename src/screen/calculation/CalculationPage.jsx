import Form from "../patient/calculation/components/Form";

/**
 * CalculationPage
 *
 * This component renders the calculation page for the user.
 *
 * @returns {React.ReactElement} The calculation page component.
 */
export default function CalculationPage() {
  return (
    <div>
      {/* The Form component renders the form for the user to input their data. */}
      <Form />
    </div>
  );
}
